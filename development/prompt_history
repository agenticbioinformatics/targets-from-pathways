## Prompt 1

> /hackathon-firstprot read the context from README.md. Write schemas.py
> defining the inter-stage data contracts for this pipeline as pydantic
> models (for the JSON manifest) and pandera DataFrameSchemas (for the
> tabular artifacts). Every later stage must validate its inputs on read and
> its outputs on write against these schemas — prose comments describing
> column names are not sufficient and have repeatedly failed on projects of
> this shape. Define at minimum: GenesSchema, GeneSetsSchema,
> InteractionsSchema, OTAssociationsSchema, Manifest [full field lists as
> specified in README.md/PROMPTS.md Stage 0]. Note in the module docstring
> that source_db is retained as a column even though only reactome is
> emitted in this version... Do not add KEGG, WikiPathways or OmniPath
> support now. Use code-improvement-advisor for code reviewing and
> improvement.

**Changes:**
- Read `README.md` and `PROMPTS.md` (Stage 0 spec) for the exact schema requirements.
- Created `schemas.py`: pandera `DataFrameSchema`s for `GenesSchema`, `GeneSetsSchema`,
  `InteractionsSchema`, `OTAssociationsSchema` (Ensembl gene-ID regex on every gene
  column, `source_db`/`sign`/`evidence_type` vocab checks, `score`/`confidence` range
  checks, composite `unique=["set_id","gene_id"]`), plus pydantic v2 models for the
  `Manifest` (nested `ResolvedTarget`, `ResolvedDisease`, `Source`, `SourceFile`,
  `OutputArtifact`, `CoverageEntry`, `ScaleReport`) and an `assert_foreign_key` helper
  for cross-table FK checks pandera can't express. Module docstring documents the
  `source_db`/future-adapter-seam decision as instructed.
- Created `test_schemas.py` (pytest) asserting each schema rejects malformed input
  (missing column, wrong dtype, out-of-vocabulary value, HGNC-symbol-as-gene_id — the
  real v1 `GRB2-1` bug class) as well as accepting valid fixtures.
- Added `pandas` to `requirements.txt`'s Stage 0 block (needed directly by
  `schemas.py`/`test_schemas.py`, not just transitively via pandera); `pytest` was
  already listed under Testing.
- Caught and fixed a real bug during self-testing: schema-wide `coerce=True` was
  silently coercing a garbage-string `directed` column to `True` instead of raising.
  Rescoped `coerce=True` to only the three numeric nullable columns that legitimately
  need it (`hierarchy_level`, `confidence`, `score`); everything else stayed strict.
- Ran the code-improvement-advisor agent on both files. It found a narrower residual
  instance of the same coercion trap (bool→float silently passing `confidence`/`score`
  range checks) and, judging a fix too complex for a case that can't occur with current
  data sources, documented it in a comment and pinned the current behavior with a test
  instead. It also added cross-field consistency validators to `CoverageEntry`
  (`percent_mapped` and `dropped_count` must agree with `raw_gene_count`/`mapped_count`)
  and ~25 additional tests (strict-mode extra-column rejection per schema,
  `assert_foreign_key` coverage, coercion-path tests, empty-dataframe acceptance).
- Verified: `python3 -m pytest test_schemas.py -v` → 42 passed.

## Prompt 2

> create or update a text file called "prompt_history" with my prompts
> (including this one) and a summary of changes associated with each prompt

**Changes:**
- Created this `prompt_history` file, logging Prompt 1 (schemas.py) and this prompt.

## Prompt 3

> Add a pytest test asserting each schema rejects a deliberately malformed
> frame (wrong dtype, missing column, sign value of 2, a gene_id that looks
> like an HGNC symbol rather than an Ensembl ID) with a clear error naming
> the offending column.

**Changes:**
- Audited `test_schemas.py` against this requirement per schema and found gaps: not
  every one of the four `DataFrameSchema`s had both a "wrong dtype" test and a
  "missing column" test (the HGNC-gene_id and sign=2 cases were already fully
  covered from Prompt 1).
- Added 5 tests to close the gaps: `test_genes_rejects_wrong_dtype_biotype`,
  `test_gene_sets_rejects_missing_column`, `test_gene_sets_rejects_wrong_dtype_set_name`,
  `test_interactions_rejects_missing_column`, `test_ot_associations_rejects_wrong_dtype_datatype_id`.
  Every schema now has a test for each of: wrong dtype, missing column, and (for
  `InteractionsSchema`, the only one with a `sign` column) sign=2, plus the
  Ensembl-gene_id check already present everywhere.
- Verified: `python3 -m pytest test_schemas.py -v` → 47 passed.

## Prompt 4

> Write a standalone Python module ingest.py that resolves, canonicalizes and
> normalizes all source data for the pipeline, validating every output
> against schemas.py (Stage 0). [full spec: CLI args including --target,
> --disease, --data-dir, --out-dir, --pathway-db, --no-download, --seed,
> --min/--max-set-size, --fi-curated-only, --fi-min-score,
> --allow-low-coverage; version-pinned acquisition with sha256 verification;
> Ensembl gene ID canonicalization off the OT target parquet; disease
> subsetting via pyarrow predicate pushdown; gene_sets/interactions
> normalization honoring --fi-curated-only/--fi-min-score; coverage_report.json
> and scale_report.json including a projected Stage 3 edge count; loud,
> non-zero-exit failures for unresolved target/disease/low coverage] as
> specified in README.md's "Open decisions"/"Open research" sections.

**Changes:**
- Before writing any code, verified every URL this module would hardcode
  against the live services (not assumed from the v1 README, which was
  explicitly flagged as unconfirmed): confirmed OT 26.06's
  `ftp.ebi.ac.uk`/`ftp://` and `https://` directory listings for
  `target/`, `association_by_datasource_indirect/`, and `disease/`;
  confirmed Reactome numbered-release URLs *do* exist
  (`https://download.reactome.org/97/...`), resolving one of README.md's
  open research questions; downloaded and byte-diffed
  `Ensembl2Reactome.txt` against `Ensembl2Reactome_PE_All_Levels.txt` to
  confirm the former is not PE-level (the exact `GRB2-1` bug class) —
  discovered it's ENSG/ENSP/ENST-mixed for human, not uniformly ENSG;
  downloaded and cross-tabulated the real FI file
  (`FIsInGene_04142025_with_annotations.txt`, 272,622 rows) to recover its
  actual 5-column schema and the exact 9-value `Direction` grammar (a 3x3
  left-cap/right-cap combination), rather than assuming the schema README.md
  flagged as unverified; discovered OT 26.06's disease table is
  MONDO-primary, not EFO-primary (README.md's own `EFO_0005755` worked
  example isn't a primary id in 26.06 at all).
- Created `ingest.py`: a pinned-registry acquisition layer (sha256+size for
  the four small single files; a runtime-discovered local lockfile for the
  two multi-GB Spark-partitioned OT directories, since their part-file UUIDs
  can't be known ahead of time) with a `--no-download` path that prints
  obtain-instructions and exits non-zero rather than guessing; Ensembl gene
  ID canonicalization off the OT target parquet (the ID authority) with
  approved-symbol-then-synonym resolution and ambiguous-symbol detection
  (kept unmapped rather than guessed); `ReactomePathways.gmt` ->
  `gene_sets.parquet`; a single swappable adapter function
  (`_load_fi_interactions`) parsing the FI file's real Direction grammar
  into signed/directed edges, honoring `--fi-curated-only`/`--fi-min-score`;
  `Ensembl2Reactome.txt` used only as an independent coverage cross-check
  (not merged into gene_sets, to avoid an unattributable many-to-one guess —
  reasoning documented in the module docstring); disease resolution with an
  EFO-id-via-dbXRefs fallback for the MONDO-primary shift; pyarrow
  predicate-pushdown disease subsetting; `coverage_report.json` /
  `scale_report.json` (including the projected Stage 3 co-membership edge
  count) and `manifest.json`; loud, non-zero-exit failures for an
  unresolvable target (with fuzzy symbol suggestions), an unresolvable
  disease, an implausibly gene-sparse disease, and <90% mapping coverage,
  all overridable except target/disease resolution.
- Made one small, deliberate `schemas.py` change beyond ingest.py itself:
  `Source.db` was restricted to `SUPPORTED_SOURCE_DBS` (`{"reactome"}`),
  which blocked recording Open Targets' own acquisition provenance in the
  manifest even though the task explicitly requires it. Added a separate
  `SUPPORTED_PROVENANCE_DBS` constant (`SUPPORTED_SOURCE_DBS | {"opentargets"}`)
  scoped to `Source.db` only — `GeneSetsSchema`/`InteractionsSchema.source_db`
  are untouched, so this doesn't relax the "reactome-only pathway curation"
  constraint, only the acquisition-provenance one. Added
  `test_manifest_accepts_opentargets_source_db` to cover it.
- Ran the full pipeline for real against live data (PTGS2 / MONDO_0008383
  "rheumatoid arthritis", the README's own PTGS2/rheumatic-disease sanity
  check, ported to 26.06's MONDO-primary id) — not a synthetic fixture. This
  caught and fixed a real bug: `pyarrow`'s `to_pandas()` returns list<struct>
  columns as `numpy.ndarray`, and `arr or []` raises `ValueError` on a
  multi-element array (ambiguous truth value) rather than doing the intended
  None-check. Also caught, via manually re-validating the written
  `genes.parquet` against `GenesSchema` with a bare `pd.read_parquet` (not
  just the in-process DataFrame Stage 1 itself validates before writing),
  that a parquet round-trip turns `list[str]` cells into `numpy.ndarray`,
  which `GenesSchema`'s `_is_list_of_str` rejected — would have broken
  "validate on read" for every stage after Stage 1. Fixed in `schemas.py`
  (accept `ndarray` alongside `list`) with a new regression test
  (`test_genes_accepts_synonyms_as_ndarray`), not worked around in ingest.py.
- Verified end-to-end against live data: resolved PTGS2 -> `ENSG00000073756`;
  resolved `MONDO_0008383` "rheumatoid arthritis" (8184 associated genes);
  6 output artifacts (`genes.parquet` 78,691 genes; `gene_sets.parquet`
  1,663 retained sets; `interactions.parquet` 208,631 curated edges;
  `ot_disease_subset.parquet`); coverage 96.46% (GMT), 99.4% (FI), 89.85%
  (Ensembl2Reactome cross-check — correctly triggers the 90% floor without
  `--allow-low-coverage`, confirming the loud-failure path); projected
  Stage 3 co-membership edge count 2,963,812 at default size caps (the
  number Stage 2/3 will need to reckon with). Confirmed manifest determinism
  (byte-identical modulo `created_at` across two identical runs) and full
  offline re-run correctness via `--no-download` against the now-populated
  `--data-dir`. Confirmed loud, correctly-worded failures for an unresolvable
  target (with real fuzzy suggestions: PTGS2/PTGS1/PTGIS/PTGES/PTGDS),
  an unresolvable disease id, a gene-sparse disease (`EFO_0000287` "biopsy
  number", 0 genetic_association genes), and a missing `--no-download`
  data-dir (prints the exact `curl` command, sha256, and size). Confirmed
  `--fi-curated-only`/`--no-fi-curated-only`/`--fi-min-score` compose
  correctly. `python3 -m pytest test_schemas.py -v` → 49 passed.
- Added `numpy` to `requirements.txt`'s Stage 0 block (now a direct
  `schemas.py` import, not just transitive via pandas/pandera).

## Prompt 5

> /hackathon-firstprot Add pytest tests for ingest.py using tiny fixture
> files under tests/fixtures/ (a handful of fake genes, pathways and
> interactions). Cover exactly three things, and skip broader coverage
> until the pipeline runs end to end: identifier mapping (clean symbol,
> deprecated/synonym symbol, unmappable symbol counted in
> coverage_report.json), schema conformance (every emitted artifact
> validates against its Stage 0 schema; a source file with a corrupt row
> fails loudly rather than being silently skipped), and manifest
> determinism (two runs over identical inputs produce byte-identical
> manifests apart from created_at/run_id; every artifact sha256 in the
> manifest matches the file on disk).

**Changes:**
- Created `tests/fixtures/`: a tiny synthetic gene universe (TP53 clean;
  PTGS2 with synonym "COX2"; FAKEGENE123 present in the GMT/FI fixtures but
  absent from the target fixture, the unmappable case) as hand-written
  Reactome-format text (`ReactomePathways.gmt.zip`,
  `FIsInGene_test_with_annotations.txt.zip`, `Ensembl2Reactome.txt` —
  zipped where the real pinned sources are zipped, so extraction is
  exercised too) plus three tiny OT-schema-accurate parquet files
  (`target.parquet`, `disease.parquet`, `association.parquet`) generated by
  a small, re-runnable `build_fixtures.py` and committed alongside it.
- Created `tests/conftest.py`: monkeypatches ingest.py's two acquisition
  entry points (`_ensure_pinned_file`, `_ensure_directory_source`) to serve
  the fixtures instead of the network/real registry — everything
  downstream (extraction, mapping, gene_sets/interactions construction,
  coverage/scale reporting, schema validation, manifest writing) runs
  unmodified. A `overrides` param lets one specific source be swapped for a
  deliberately corrupt file while every other source stays the normal
  fixture, for the corrupt-row tests.
- Created `tests/test_ingest_identifier_mapping.py`,
  `test_ingest_schema_conformance.py`, `test_ingest_manifest_determinism.py`
  covering exactly the three requested areas (7 tests total).
- Ran the suite for real before treating it as done (per this skill's Phase
  5), which caught two real problems, not just fixture-setup mistakes:
  - `Python 3.9` doesn't accept `dict | None` without
    `from __future__ import annotations` — a straight syntax bug, fixed in
    `tests/conftest.py`.
  - The malformed-FI-row test initially passed for the *wrong* reason: a
    non-zipped bad-FI fixture raised `zipfile.BadZipFile` before ever
    reaching row-parsing at all. Fixing that (wrapping the bad row in a
    proper single-member zip, matching the real registry format) surfaced
    a genuine gap in `ingest.py` itself: pandas' C parser does **not**
    raise on a short/ragged row — it silently pads missing trailing fields
    with `NaN`, which then misses every `_FI_DIRECTION_CODES` key and gets
    dropped with only a logged warning, not a hard failure — exactly the
    "silently skipped" failure mode Stage 1 is supposed to prevent. Fixed
    in `ingest.py`'s `_load_fi_interactions`: an explicit null-check across
    the 5 required FI columns right after `read_csv`, `_fail()`-ing with
    the offending row before any processing, rather than letting a
    malformed row survive as silent `NaN`-driven attrition. Manually
    verified both corrupt-row tests raise for the intended reason (by
    running them outside pytest with logging enabled and reading the
    actual error message), not an incidental crash elsewhere.
- Verified: `python3 -m pytest` (both `test_schemas.py` at repo root and
  the new `tests/` directory together) → 56 passed. `tests/__pycache__` is
  already covered by the existing `*cache*` `.gitignore` rule.

**How to run:** `python3 -m pytest` from `v2/` (needs `pyarrow`/`pandas`/
`pandera`/`pydantic` per `requirements.txt`; no network access required —
`tests/` never touches the pinned registry).

## Prompt 6

> /hackathon-firstprot Write gsea_discovery.py that takes only the Stage 1
> --manifest and reads the normalized artifacts it points to — never a raw
> source file. Retrieve disease-associated genes from
> ot_disease_subset.parquet using ONLY the genetic_association datatype
> (not the aggregated/overall score — this matters for avoiding
> circularity later), and run Gene Set Enrichment Analysis of those genes
> against the gene_sets.parquet sets (use an existing GSEA library such as
> blitzgsea, don't reimplement GSEA). Gene sets arrive already size-capped
> by Stage 1; before testing, additionally collapse near-duplicate sets
> (Jaccard > 0.7, keeping the smaller/more specific set) because
> ReactomePathways.gmt is an all-hierarchy-levels file in which ancestor
> and descendant sets are heavily redundant, which makes
> Benjamini-Hochberg anticonservative and inflates the significant-pathway
> count. Apply BH FDR correction across all tested pathways, and output a
> TSV of disease-relevant pathways with p-value, FDR, gene set, and
> source_db, filtered by user-specified --pval-threshold and
> --fdr-threshold CLI args (defaults 0.05 and 0.1). Also compute the set
> of pathways containing the --target gene from the manifest and include
> it in the output (a contains_target boolean column). Log the count of
> sets tested before and after collapsing.
>
> Correcting for annotation bias is an open decision — see "Open decision
> — correcting Stage 2 for annotation bias" in README.md. BH-FDR alone is
> option C of three, chosen as a placeholder. Well-studied genes carry
> both more Open Targets evidence and more Reactome annotation, so
> enrichment is inflated systematically and the significant-pathway count
> will likely be large (v1 returned 250). Structure the code so a
> per-pathway specificity score can be added as an extra column and filter
> without reshaping the output. Do not resolve this decision yourself.

**Changes:**
- Created `gsea_discovery.py` (Stage 2): reads only `--manifest`, resolving
  `gene_sets.parquet`/`ot_disease_subset.parquet` via
  `manifest.output_artifacts` (falling back to a path alongside
  `manifest.json` if the stored path isn't valid from the current cwd) —
  never a raw source file. Builds the GSEA ranking signature from
  `ot_disease_subset`'s `genetic_association` rows only (max score per gene
  across datasources, never the aggregated/overall score), collapses
  near-duplicate gene sets (Jaccard > 0.7, inverted-gene-index greedy pass,
  smallest-first so the more specific set is always kept) before calling
  `blitzgsea.gsea` for preranked GSEA — BH-FDR comes from blitzgsea's own
  `statsmodels`-backed correction rather than being reimplemented. Filters
  on `--pval-threshold`/`--fdr-threshold` (both required, AND-combined),
  adds `contains_target`, writes `disease_pathways.tsv`. `run_gsea` returns
  every tested pathway's stats unfiltered specifically so a future
  specificity-score column can slot in before `filter_results` without
  reshaping either function — per the open-decision instruction, that
  score itself was not added.
- Built a hand-crafted, schema-conformant example
  (`example_data/build_gsea_example.py` → `example_data/stage1_run/`, 18
  genes / 5 pathways) rather than downloading real data, since real Stage 1
  runs need network access. Iterated on the synthetic gene scores/pathway
  membership by actually running the script until it demonstrably exercised
  the ancestor/child near-duplicate collapse, a genetic_association-only
  decoy row (proving the aggregated score isn't used), and a real
  threshold-excluded "scattered" pathway.
- Self-review (Phase 6) caught and fixed: a stray no-op column rename left
  over from drafting, a `run_gsea` return-type annotation that said
  `pd.DataFrame` but actually returned a tuple, and unrounded
  scientific-notation floats in the TSV (rounded only at write time, not in
  the filtering logic); added a short comment explaining the
  one-directional (non-expression) GSEA signature design choice.
- Documented setup/run/expected-output in a new README.md "Running Stage 2"
  section.
- Verified end-to-end against the example data (`python3 gsea_discovery.py
  --manifest example_data/stage1_run/manifest.json`).

## Prompt 7

> Add a pytest test for gsea_discovery.py using a small synthetic gene set
> fixture (e.g. 20 genes, 5 pathways, with a pathway you construct to be
> deliberately enriched) that verifies: the deliberately-enriched pathway
> is returned with p-value below threshold, FDR correction is actually
> being applied (test with a fixture where an individually-significant
> pathway becomes non-significant after correction), pathways not meeting
> threshold are excluded from output, and that two near-identical gene
> sets (Jaccard > 0.7) are collapsed to one before testing.

**Changes:**
- Added `tests/test_gsea_discovery.py` (8 tests), calling
  `gsea_discovery.py`'s functions directly on hand-built DataFrames — no
  manifest/parquet round-trip needed for unit-level GSEA logic.
- Grew the fixture to 25 genes / 9 pathways (past the suggested 20/5) after
  hitting two real obstacles discovered by actually running the code: a
  20-gene signature triggers a `ZeroDivisionError` inside blitzgsea's own
  calibration (an anchor gene-set-size coincides with the full signature
  length), and a middle-ranked cluster needs several "noise" pathways
  padding the tested library before Benjamini-Hochberg has enough multiple-
  testing burden to actually flip a borderline pathway's significance.
  Tuned gene-score bands and cluster placement empirically (repeated real
  `blitzgsea` runs, not hand-derived math) until: `strong_pathway` is
  unambiguously significant; `borderline_pathway` is individually
  significant (p < 0.05) but excluded once BH-FDR corrects across the full
  tested library (fdr > 0.1); 6 scattered noise pathways are tested but
  excluded; `dup_ancestor`/`dup_child` (Jaccard 0.833) collapse to the
  smaller child, confirmed absent even from the *unfiltered* result
  (collapsed before testing, not merely filtered out afterward).
- Discovered and documented that blitzgsea's seeded output is not
  bit-for-bit deterministic across runs (multiprocessing-pool jitter,
  confirmed via 5 repeated runs of the same fixture) — chose fixture
  margins wide enough to hold regardless, and wrote every assertion as a
  threshold/inequality check rather than an exact-float pin.
- Added a bonus test for `build_signature()`'s `genetic_association`-only
  filtering (a decoy `affected_pathway` row must be ignored).
- Verified: `python3 -m pytest` → 64 passed, stable across 5 repeated runs.

## Prompt 8

> /hackathon-firstprot Modify gsea_discovery.py to write its pathway list
> as a stable, documented TSV schema, and add a --benchmark-holdout-file
> optional CLI arg: a list of genes (resolved to Ensembl IDs via Stage 1's
> genes table, not matched on symbol) to exclude from the
> disease-associated seed gene set before running GSEA. This will be used
> by Stage 7's benchmark validation to prevent circularity when known
> resistance-pair genes overlap with disease seed genes.

**Changes:**
- Added `schemas.DiseasePathwaysSchema` (`set_id`, `gene_set`, `source_db`,
  `pval`, `fdr`, `contains_target`) to `schemas.py`, matching the project's
  existing pandera-artifact-contract convention; `gsea_discovery.py` now
  derives `OUTPUT_COLUMNS` from the schema itself (single source of truth
  for column set *and* order) and validates `disease_pathways.tsv` against
  it immediately before writing.
- Added `--benchmark-holdout-file`: resolves gene symbols/synonyms/Ensembl
  IDs (one per line, `#` comments allowed) via Stage 1's `genes.parquet`,
  reusing `ingest.py`'s own `resolve_target`/`_build_symbol_index` rather
  than any new resolution logic, and excludes resolved genes from
  `ot_disease_subset` before `build_signature` runs; `genes.parquet` is
  only loaded when the flag is actually passed. Unresolvable entries fail
  loudly, matching the project's established failure-mode discipline.
- Extended `example_data/build_gsea_example.py` to also emit
  `genes.parquet` (with a deliberate synonym case, `OLDG17`) and added
  `example_data/benchmark_holdout_example.txt`; verified end-to-end that
  the holdout demonstrably drops a pathway below blitzgsea's minimum
  tested-set size rather than merely losing significance.
- Self-review caught and fixed a real gap: a missing
  `--benchmark-holdout-file` path previously raised a raw
  `FileNotFoundError` traceback instead of a loud, well-worded failure.
- Also caught, while re-verifying, that the README's "Expected output"
  section (written in Prompt 6) hardcoded one run's exact p-values, which
  jitter run-to-run (the same blitzgsea nondeterminism documented in Prompt
  7) — fixed the README to show approximate magnitudes and state explicitly
  what actually is stable (which pathways, their order, `contains_target`).
- Verified: `python3 -m pytest` → 64 passed, unaffected by the changes.

## Prompt 9

> commit this

**Changes:**
- Reviewed `git status`/`git diff`, confirmed `example_data/stage1_run/`'s
  generated artifacts are intentionally committed (same convention already
  established for `tests/fixtures/`), and committed `README.md`,
  `schemas.py`, `gsea_discovery.py`, `example_data/`, and
  `tests/test_gsea_discovery.py` as commit `966ae89`.

## Prompt 10

> create or update a text file called "prompt_history" with my prompts
> (including this one) and a summary of changes associated with each
> prompt

**Changes:**
- Appended Prompts 6-10 to this file, covering `gsea_discovery.py`'s
  initial implementation, its pytest suite, the TSV-schema/
  `--benchmark-holdout-file` follow-up, the commit, and this update.

## Prompt 11

> explain the error when running ingest.py: "ERROR ingest: Failed to
> download
> https://reactome.org/download/tools/ReactomeFIs/FIsInGene_04142025_with_annotations.txt.zip:
> HTTP Error 403: Forbidden"

**Changes:**
- No code investigated blind: fetched the failing URL directly with both
  bare `urllib.request.urlopen()` and `curl`, confirming the 403 was
  `reactome.org`'s Cloudflare bot protection rejecting `urllib`'s default
  `Python-urllib/x.y` User-Agent specifically — `curl` (any UA) got `200`
  with the exact pinned `content-length`. Checked all four of `ingest.py`'s
  external hosts the same way; only `reactome.org` (host of the FI file)
  rejected the bare request.
- Fixed `ingest.py`: added a small `_urlopen()` helper sending a normal
  browser-like `User-Agent`, and switched both `_download_to` and
  `_list_apache_index` to use it instead of calling `urllib.request.urlopen`
  directly, so the fix isn't specific to the one host that happened to
  trigger it. Verified the FI URL now returns `200` with the pinned
  `1182418`-byte content-length. `python3 -m pytest` → 64 passed
  (unaffected — the suite mocks the network layer entirely).

## Prompt 12

> commit this

**Changes:**
- Committed `ingest.py`'s User-Agent fix alone as `c68a99c`. Left the
  untracked `run1_data/` (634MB of real downloaded Reactome/Open Targets
  data from the user's own `ingest.py` run) alone — working data, not
  something to commit.

## Prompt 13

> /hackathon-firstprot Write build_graph.py that takes Stage 2's
> disease-relevant pathway list and the target's own pathways, and
> constructs a pathway-based gene-gene graph — use this exact term in the
> module docstring, log messages, and variable names, not the generic
> "gene-gene graph". Build it as a networkx.DiGraph: nodes are genes keyed
> by Ensembl gene ID; edges come from two sources — (a) co-membership in
> at least one pathway from the union of (disease-relevant pathways) ∪
> (target-containing pathways), added as a bidirectional pair of edges
> with sign=0 (unknown effect); and (b) directional, signed edges read
> straight from Stage 1's interactions.parquet (already parsed, filtered
> and canonicalized — do not re-parse the Reactome FI file here). Tag
> every edge with source_db. Which curation those edges come from is an
> open decision (see README.md, "Open decision — what counts as pathway
> topology?"); this stage must work unchanged whichever option is chosen,
> since it only ever reads the normalized table.
>
> Weight co-membership edges by 1 / (|pathway| - 1) (or another documented
> decreasing function of set size), summed over the pathways that induced
> them. Without this, a single 200-gene pathway contributes ~20,000 edges
> and a handful of large Reactome sets determine the entire graph's
> topology, which is the failure mode behind v1's hub-dominated results.
> Compare the realized edge count against the projection in Stage 1's
> scale_report.json and log both.
>
> Represent the graph as a scipy.sparse adjacency matrix plus a gene index
> for anything downstream that iterates over all edges (Stage 5's
> diffusion in particular); keep networkx for construction, inspection and
> small test fixtures only. GraphML serialization of a million-edge graph
> is slow and large — serialize the sparse matrix plus the index instead,
> and say so in the docstring.
>
> Record the Reactome version and the random seed from the Stage 1
> manifest as graph metadata.

**Changes:**
- Created `build_graph.py` (Stage 3): builds the pathway-based gene-gene
  graph as a `networkx.DiGraph` over (Stage 2's disease-relevant pathways)
  ∪ (the target's own pathways, pulled from `gene_sets.parquet` — the "no
  additional cap" placeholder from README's open decision #3, since
  `gene_sets.parquet` is already Stage 1-capped). Co-membership edges:
  bidirectional, `sign=0`, weight `1/(|pathway|-1)` summed over qualifying
  pathways sharing a pair. Interaction edges: read straight from
  `interactions.parquet`, **deliberately scoped to pairs where both genes
  are already in the pathway union** — a design decision not explicit in
  the prompt, reasoned from README's framing ("spanning the target's and
  disease's pathway neighborhoods") to avoid silently reintroducing the
  whole disease-agnostic interactome. Duplicate `(gene_a, gene_b)` rows in
  `interactions.parquet` (legitimate — distinct FI reaction annotations)
  collapsed to the highest-confidence row, since a plain `DiGraph` can't
  hold parallel edges — also not explicit in the prompt, found by tracing
  `ingest.py`'s actual dedup logic. Serialized as two aligned
  `scipy.sparse` CSR matrices (`weight` = `comembership_weight +
  confidence`, `sign`) plus a gene-index JSON and a `graph_metadata.json`
  recording the manifest's seed and Reactome source versions — not
  GraphML.
- Paused after implementing, before running/reviewing/documenting
  (Phases 5-8): flagged the two undocumented design decisions above and
  asked whether to proceed, since the session had just exited auto mode.

## Prompt 14

> /hackathon-firstprot Add a pytest test with a small synthetic pathway
> list plus a small synthetic interactions table (a handful of directed,
> signed relations) and hand-verify the expected edge list; assert
> build_graph.py produces exactly those directed, signed edges with
> correct source_db tags, that co-membership edge weights match
> hand-computed 1 / (|pathway| - 1) values for a fixture containing one
> small and one large pathway, and that a gene appearing in only one
> pathway alone (no shared pathway with any other gene) is an isolated
> node, not silently dropped.

**Changes:**
- Created `tests/test_build_graph.py` (4 tests): a hand-built fixture with
  `R-SMALL` (2 genes) and `R-LARGE` (4 genes, overlapping `R-SMALL` in one
  pair so its weight is a *sum* of both pathways' contributions),
  `R-ISOLATE` (1 gene alone), and interactions deliberately touching only
  one direction of each co-membership pair (so the untouched reverse
  direction proves the `sign=0` default survives independently) plus one
  out-of-scope row (`Z`, in no pathway) that must be dropped. Asserted the
  exact 12-edge directed/signed edge list, the weight formula for both
  pathway sizes including the summed case, the isolated node's presence at
  degree 0, and a light cross-check that `graph_to_sparse`'s matrices agree
  with the networkx-level attributes.
- Sanity-checked the test wasn't vacuous by deliberately corrupting an
  expected sign and confirming it failed loudly, then reverted (the file
  wasn't yet tracked by git, so this had to be done by hand rather than via
  `git checkout`).
- `python3 -m pytest` → 68 passed.

## Prompt 15

> commit this

**Changes:**
- Committed `build_graph.py` and `tests/test_build_graph.py` as `2274bc7`.

## Prompt 16

> /hackathon-firstprot Write genetic_evidence_weights.py that takes the
> Stage 3 graph (with its target node) and the Open Targets association
> data, and computes per-gene genetic-evidence scores restricted
> explicitly to non-pathway- derived datatypes (genetic_association,
> known_drug — exclude affected_pathway and any literature/pathway-derived
> datatype — note that OT 26.06 ships an evidence_reactome datasource
> feeding affected_pathway, so this exclusion is load-bearing, not
> theoretical: without it the pipeline would weight its graph with
> Reactome-derived evidence and then validate Reactome pathway structure
> against itself). Add a --datatypes CLI arg (comma-separated) defaulting
> to genetic_association,known_drug so the exclusion is explicit and
> overridable, with a code comment explaining why affected_pathway is
> excluded by default (orthogonality with Stages 2-3). Map these scores
> onto the graph as node weight attributes (genetic_evidence_score,
> default 0 for genes with no matching evidence), and derive edge weights
> by default via a --edge-weight-mode CLI arg (default avg, also
> supporting product, of the two endpoint node weights — both node and
> edge weighting are on by default, not optional). Output the updated
> graph in the same serialization format Stage 3 produces, plus a plain
> TSV of per-gene weights for inspection.

**Changes:**
- Created `genetic_evidence_weights.py` (Stage 4): `--datatypes` (default
  `genetic_association,known_drug`) is an inclusion allowlist, so
  `affected_pathway` is excluded simply by omission; the default constant's
  comment cites the concrete `reactome` -> `affected_pathway` mapping
  already in `ingest.py`'s `_DATASOURCE_TO_DATATYPE`, per the prompt's
  instruction. Per-gene score = max across admitted-datatype rows (mirrors
  `gsea_discovery.py.build_signature`'s convention), 0.0 default for genes
  with no evidence — verified a gene outside the graph's node set is
  correctly ignored even with OT evidence. Edge weight
  (`--edge-weight-mode avg|product`) derived purely from the two endpoint
  node scores at Stage 3's existing edge positions.
- Added `schemas.GeneWeightsSchema` for the output TSV, matching the
  established per-artifact-schema convention (not explicitly requested
  this time, but consistent with house style).
- Closed a gap from earlier turns: `build_graph.py` (Stage 3) had only
  ever been unit-tested, never actually run end-to-end. Extended
  `example_data/build_gsea_example.py` with `interactions.parquet`
  (including one deliberately out-of-scope row) and ran Stages 2->3->4 as
  a real chain against it, verifying every intermediate number by hand
  (edge weights, node scores, metadata) against the actual files on disk.
- Review caught and fixed one real finding: `gene_weights.tsv` wrote
  unrounded floats (harmless with hand-picked clean example values, but
  unreadable on real OT scores) — rounded only at TSV-write time, matching
  the identical fix already made in `gsea_discovery.py`.
- Added the missing "Running Stage 3"/"Running Stage 4" README sections.
  `python3 -m pytest` → 71 passed. Left uncommitted, and flagged that no
  pytest test file existed yet for the new script.

## Prompt 17

> /hackathon-firstprot Add a pytest test with a small synthetic pathway
> list plus a small synthetic interactions table (a handful of directed,
> signed relations) and hand-verify the expected edge list; assert
> build_graph.py produces exactly those directed, signed edges with
> correct source_db tags, that co-membership edge weights match
> hand-computed 1 / (|pathway| - 1) values for a fixture containing one
> small and one large pathway, and that a gene appearing in only one
> pathway alone (no shared pathway with any other gene) is an isolated
> node, not silently dropped.

**Changes:**
- Recognized this as a verbatim repeat of Prompt 14, already fulfilled by
  the existing, still-passing `tests/test_build_graph.py` (committed in
  `2274bc7`) — made no code changes, and asked whether a different test
  (e.g. for the still-untested `genetic_evidence_weights.py`) was intended
  instead.

## Prompt 18

> /hackathon-firstprot Add a pytest test with a small fixture Open Targets
> association table containing a mix of datatypes for a few genes on a
> small synthetic graph; verify that node weights only reflect the allowed
> datatypes, that a gene with only affected_pathway evidence gets weight 0
> by default, and that edge weights (when enabled) are computed correctly
> from endpoint node weights for a hand-picked edge.

**Changes:**
- Created `tests/test_genetic_evidence_weights.py` (3 tests) calling
  `genetic_evidence_weights.py`'s two pure functions directly against a
  hand-built OT association fixture: gene A with both allowed
  (`genetic_association`=0.9, `known_drug`=0.5) and a decoy disallowed
  (`affected_pathway`=0.99) row (max of *allowed* rows must win, never the
  higher decoy); gene C with *only* `affected_pathway` evidence (must get
  0.0, not 0.99); gene D with no rows at all (0.0 via the other path). A
  small synthetic chain graph (`A<->B<->C<->D`, as a raw sparse matrix +
  index) to hand-verify `avg`/`product` edge weights, including the subtler
  case of a real edge where both endpoints score 0 — confirmed it stays a
  present, explicit zero rather than silently vanishing.
- Sanity-checked via the same corrupt-and-confirm-failure technique as
  Prompt 14, then reverted. `python3 -m pytest` → 71 passed (3 new).

## Prompt 19

> Ensure genetic_evidence_weights.py writes the weighted graph in exactly
> the same format Stage 3 produces, so score_candidates.py (Stage 5) can
> load either the unweighted Stage 3 graph or this weighted graph
> interchangeably via the same --graph argument. Document the node/edge
> weight attribute names as a comment at the top of the script.

**Changes:**
- Recognized this as a correction to Prompt 16's design: writing two extra
  files (`graph_node_genetic_evidence_score.npy`,
  `graph_edge_genetic_evidence_weight.npz`) broke true interchangeability,
  since a future Stage 5 loader would need branching logic to detect which
  stage produced a directory. Redesigned `genetic_evidence_weights.py` to
  write **exactly** Stage 3's four filenames: `graph_weight.npz` is now
  *replaced* with the genetic-evidence-derived edge weight at Stage 3's
  original sparsity pattern (no edge added or removed, only re-weighted);
  `graph_sign.npz`/`graph_gene_index.json` copied through unchanged;
  `graph_metadata.json` extended (not replaced) with a
  `genetic_evidence_score` gene->score mapping. Removed the two extra
  files. Added the requested attribute-name comment block immediately
  after the module docstring, before any imports.
- Proved interchangeability concretely rather than asserting it: ran
  `load_stage3_graph` — the one loader function — against both a pure
  Stage-3-only directory (no `genetic_evidence_score` key) and this
  Stage-4-processed directory (key present), confirming identical code
  handles both with no branching.
- Flagged one real tradeoff explicitly rather than deciding it silently:
  this design loses Stage 3's original structural weight when Stage 4 runs
  in place (the default); noted the `--out-dir` escape hatch to preserve
  both, and asked the user to confirm this was the intended tradeoff versus
  a combined (structural + genetic-evidence) weight.
- Updated README's Stage 4 section to match. `python3 -m pytest` → 71
  passed (the 3 tests from Prompt 18 test pure functions unaffected by the
  I/O redesign).

## Prompt 20

> commit this

**Changes:**
- Committed `genetic_evidence_weights.py`, `tests/test_genetic_evidence_weights.py`,
  `schemas.py` (`GeneWeightsSchema`), the README updates, and the extended
  `example_data/` generator + regenerated example artifacts together as
  `1a3b087`.

## Prompt 21

> create or update a text file called "prompt_history" with my prompts
> (including this one) and a summary of changes associated with each
> prompt

**Changes:**
- Appended Prompts 11-21 to this file, covering the `ingest.py` 403/
  User-Agent fix, Stage 3 (`build_graph.py`) and its tests, Stage 4
  (`genetic_evidence_weights.py`), its tests, and its interchangeable-
  serialization-format redesign, the intervening commits, and this update.

## Prompt 22

> rename pipeline scripts, add stage 1,2,3... etc at the beginning of each
> file, according to README. put all pipeline scripts in a new directory
> called "pipeline/"

**Changes:**
- Created `pipeline/` and `git mv`'d the five stage scripts into it with a
  `stageN_` prefix taken from the README pipeline overview:
  `schemas.py` -> `pipeline/stage0_schemas.py` (README "Stage 0"),
  `ingest.py` -> `pipeline/stage1_ingest.py`,
  `gsea_discovery.py` -> `pipeline/stage2_gsea_discovery.py`,
  `build_graph.py` -> `pipeline/stage3_build_graph.py`,
  `genetic_evidence_weights.py` -> `pipeline/stage4_genetic_evidence_weights.py`.
- Updated the inter-stage imports accordingly (`from schemas import` ->
  `from stage0_schemas import`, `from ingest import` -> `from
  stage1_ingest import`). Running scripts as `python3 pipeline/stageN_*.py`
  still works because Python puts the script's own dir on `sys.path`.
- `tests/conftest.py` now inserts `pipeline/` (not the repo root) on
  `sys.path` and imports `stage1_ingest as ingest`; every test module's
  import line updated to the new module names. Moved the one stray
  root-level test, `test_schemas.py`, into `tests/` so it picks up that
  conftest.
- Rewrote every remaining `*.py` filename mention in docstrings, comments,
  `argparse` `prog=`/help strings, error hints ("run build_graph.py
  first?"), the `_USER_AGENT` string, `example_data/build_gsea_example.py`,
  and README's "Running Stage 2/3/4" command blocks and prose to the new
  `pipeline/stageN_*.py` names. Logger names (`getLogger("ingest")` etc.)
  left unchanged — they are not filenames and README's expected-output log
  samples depend on them.
- Test/reference file names (`tests/test_build_graph.py`, ...) left as-is;
  they are not pipeline scripts.
- `python3 -m pytest` -> 71 passed. Smoke-tested all four stage scripts'
  `--help` and re-ran the `example_data/` generator through Stages 2-4.

## Prompt 23 — docs split, --help, development/ + dev skill

> /hackathon-cleanup: Give README.md a "Quick start" section (how to run
> each step, two sentences each); create docs/ with the detailed per-step
> user guide + the full CLI invocation chain; add a --help description to
> every stage script's argparse; create development/ holding PROMPTS,
> prompt_history and a README about the dev process, plus
> development/hackathon-pipeline-dev/SKILL.md — an agentic skill about what
> was learned. (Skill later asked to be generic to any bioinformatics
> pipeline given a problem + pipeline description.)

Note: prompt_history was not maintained between Prompt 22 and here. The
intervening work (Stage 3-7 implementation, benchmark split into
benchmarking/, the score_candidates/annotate_context renames,
example_data/stage1_run -> example_run, Stage 8 -> Stage 7, log trimming,
the end-to-end test) is recorded in the git history and in README.md's
numbered "Plan update" sections.

**Changes:**
- `README.md`: replaced the eight "Running Stage 0-7" sections (~510 lines)
  with a compact **Quick start** — install, one-command `run_pipeline.py`,
  offline demo, a 7-row per-stage table (two-sentence descriptions), the
  manual stage-by-stage command chain, and pointers to docs/, benchmarking/,
  development/. The plan content (Introduction, Pipeline overview, Plan
  updates, Open decisions, Development plan, Testing steps) is unchanged.
- New `docs/README.md`: the detailed per-stage reference, moved verbatim
  from README.md's old "Running Stage N" sections, prefixed with a
  "Full run — the CLI invocation chain" section (one-command + manual +
  offline). `benchmarking/` links rewritten to `../benchmarking/`.
- `pipeline/stage1_ingest.py`: added `help=` to `--seed` / `--min-set-size`
  / `--max-set-size` (the only args repo-wide without it). Enriched the
  `argparse` `description` on stages 1-7 to name each stage's input(s) and
  output file(s), so `python3 pipeline/<script> --help` is self-documenting.
- `PROMPTS.md` -> `development/PROMPTS.md`, `prompt_history` ->
  `development/prompt_history` (git mv). Fixed the garbled
  "Documentation:" prompt and the stray loose line; added a "Development
  docs:" prompt; rewrote relative links (`../README.md`,
  `../benchmarking/PROMPTS.md`).
- New `development/README.md`: how the pipeline was built (plan-first,
  schema-first, one-script-per-stage, synthetic example, test-as-you-go,
  six plan updates, late bulk renames).
- New `development/hackathon-pipeline-dev/SKILL.md`: an agentic skill —
  eight generic patterns for turning a problem statement + pipeline
  description into a modular, reproducible, tested bioinformatics pipeline
  (any input data / databases). Deliberately free of this project's
  specifics.
- Stale doc refs fixed: `tests/test_schemas.py` and
  `pipeline/stage6_annotate_context.py` docstrings (`PROMPTS.md` ->
  `development/PROMPTS.md`; "Stage 8" -> "Stage 7 report").
- `python3 -m pytest` -> 100 passed. All eight stage scripts' `--help`
  render.
