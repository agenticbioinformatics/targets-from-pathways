# Stage 0 — shared schemas (inter-stage data contracts)
pydantic>=2.0
pandera
pandas
numpy

# Stage 1 — ingest & canonicalize (download, parquet I/O, ID mapping)
pyarrow
pandas

# Stage 2 — disease-pathway discovery (GSEA, FDR correction)
blitzgsea
statsmodels

# Stage 3 — pathway-based gene-gene graph construction
networkx
scipy

# Stage 4 — genetic-evidence weighting
pandas
pyarrow

# Stage 5 — candidate scoring (topology + random-walk-with-restart)
networkx
numpy
scipy

# Stage 6 — contextual annotation & filtering (reads the OT `target` parquet)
pandas
pyarrow

# Stage 7 — output/report (pipeline/run_pipeline.py orchestrator +
# pipeline/stage7_report.py, which writes a single self-contained HTML file
# using only the standard library for templating — no web framework)
pandas
pyarrow

# Benchmark validation (see benchmarking/) — its own module, not a pipeline stage
pandas
numpy
scipy

# Testing (all stages)
pytest
