# Stage 0 — shared schemas (inter-stage data contracts)
pydantic>=2.0
pandera
pandas
numpy

# Stage 1 — ingest & canonicalize (download, parquet I/O, ID mapping)
pyarrow
pandas

# Stage 2 — disease-pathway discovery (GSEA, FDR correction)
blitzgsea
statsmodels

# Stage 3 — pathway-based gene-gene graph construction
networkx
scipy

# Stage 4 — genetic-evidence weighting
pandas
pyarrow

# Stage 5 — candidate scoring (topology + random-walk-with-restart)
networkx
numpy
scipy

# Stage 6 — contextual annotation & filtering
pandas

# Stage 7 — benchmark validation
pandas
numpy
scipy

# Stage 8 — output/report + web UI
streamlit

# Testing (all stages)
pytest

# Non-pip dependencies:
# - Flask is a documented alternative to Streamlit for Stage 8's web UI
#   (README/PROMPTS.md leave this an open choice); if Flask is picked instead,
#   swap the `streamlit` line above for `flask`.
